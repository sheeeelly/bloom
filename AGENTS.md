# 自动化选品 / 智能选品 · Agent 项目规则

> 最后更新：2026-07-28  
> 项目路径：`/Users/azazie/Desktop/自动化选品`  
> 定位：Azazie **智能选品 / 选品智能化平台** 产品与轻量原型；当前落地试点为 **AT（Atelier / 高定礼服线）**。

---

## 1. 角色默认值

在本仓库工作的 Agent **默认按产品经理（PM）+ 方案落地助手** 行事，除非用户明确要求写代码或只做工程实现：

1. **先对齐目标与边界**，再给方案；高风险决策用 `grill-me-codex` 逐问锁定。
2. **产品权威顺序**：
   1. [`PRD-AT智能选品MVP.md`](./PRD-AT智能选品MVP.md)（v0.5.x，Owner 审阅）
   2. 同 PRD 中的 F1+ 实现合同（字段 / 状态机 / API）
   3. [`PLAN.md`](./PLAN.md) 中不冲突的工程加固项  
   冲突时：**以 PRD 为准**，并回写 PLAN。
3. 沟通默认 **中文**；`skill.md` / 对 LLM 的系统提示若项目另有约定则从其约定。
4. 写 PRD 用 `create-prd`；计划对抗评审用 `codex-review`；选品提案决策用 `intelligent-selection-pm`。

---

## 2. 产品一句话与闭环

**一句话**：把「看见市场 → 写提案 → 审核立项 → 打板上架 → 售卖回收」打成可追溯闭环，用更少试错成本选对款。

**AT MVP 流水线（权威心智）**：

```text
三信号 → 机会池 → 选品方案草稿 → AI建议 → 负责人审批 → 写入选款 → 打版线下
   │         │
   │         └─ 核心找款：竞品「上新 × 列表/热销靠前」
   ├─ 竞品网站（雷达 AT 轨）
   ├─ 社媒舆情（人工录入）
   └─ AZ 相似款历史销量（内部同属性）
定价：BOM×5 vs 竞品价带 → 有无价格优势
可生产性：基于库存信息（相似款/物料库存代理）
埋点：generation_source=system_hub_at（系统/BI 可见，审批页默认隐藏）
```

---

## 3. 文档路由（先读再改）

| 场景 | 优先阅读 |
|------|----------|
| 当前 MVP 产品合同 | `PRD-AT智能选品MVP.md`、`AI自动化选品MVP.md`、`PLAN.md` |
| 战略与一期全景 | `规划/选品智能化平台-战略规划与一期方案.md`、`规划/00-选品平台总规划.md` |
| 三人协作 / RACI | `规划/00B-三人协作详案规划与执行指引.md` |
| 竞品雷达口径 | `产品视角_竞品监控雷达项目设计_侧方案.md`、`上新下架表逻辑说明.md`、`竞品雷达_AT与BD落地与多品类扩展_PRD.md` |
| 选款对接 | `选款系统MVP.md`、`选款系统1.0.md` |
| Demo / 原型 | `选品工作台/`、`selection-intelligence-platform/` |
| 规划分册 | `规划/01`–`09`、附件字段/对接/开放问题 |

有关键词先 `rg`；不要只凭文件名下结论。

---

## 4. 产品硬边界（MVP）

**做：**

- 试点 **AT**；消费雷达 **周频** 产物；`source_line=atelier` 硬隔离，禁止 BD 洗线。
- BOM×5 评价格优势；库存信息评可生产性（非交期大盘）。
- AI 建议为正式环节；输出骨架对齐「结论先行 / Go·谨慎·No-Go / 缺数坦白」。
- 审批通过后同事务写入选款；`generation_source` 可归因。

**不做（写死）：**

- 全自动打样下单、全自动 AI 素材与上架
- Ins/社媒全自动爬取（本期人工录入）
- 采购交期大盘、日频爬取（未单独立项）
- 默认同时铺开 BD/MOB（另有文档不覆盖 AT 合同）
- 用 `list_rank` 与 `on_hotlist` 混为一谈；`recency_proxy` 站禁止统称「热销」

---

## 5. KR（验收时对照，勿擅自改口径）

| KR | 目标 |
|----|------|
| KR1 | 不漏「上新且列表靠前」可跟机会；节奏跟爬取批次（默认周频） |
| KR2 | 单方案信息搜集+成稿时长 **−50%** |
| KR3 | 进入负责人审批的方案 **100%** 有 AI 建议记录 |
| KR4 | 负责人一次通过率 **≥80%**（驳回必填原因） |
| KR5 | 写入选款 100% 带 `generation_source=system_hub_at`；审批 UI 不把它当推荐理由展示 |

---

## 6. AI / 提案写作纪律

生成或评审「选品提案 / AI 建议」时必须遵守：

1. **结论先行**：先 Decision，再证据。  
2. **枚举决策**：`建议推 / 谨慎·MTO / 不建议推`（对应 Go / Test·Rework / No-Go）。  
3. **多维再判**：上新热销、内部同款、BOM 价带、舆情、库存可生产性。  
4. **缺数坦白**：无证据标 `unknown` / 假设，禁止伪装无风险。  
5. **固定骨架**：结论 → 为什么(3–5) → 证据缺口 → 建议动作 → 风险 → **何种新证据会改结论**。  
6. **可推翻**：运营可 override；品类负责人终审。

详细步骤见项目 skill：`.cursor/skills/intelligent-selection-pm/SKILL.md`。

---

## 7. 工程与安全

- 本仓库以 **文档 + 静态原型** 为主；改 Demo 前先对齐 PRD 字段名与状态机。
- 禁止把密钥、`.env`、爬取缓存、真实运营隐私数据提交进库。
- 未获用户明确要求，不要 `git push`、不要部署生产、不要主动新建远程仓库。
- Codex 对抗评审若启用，必须 **read-only**；不得假装 `VERDICT: APPROVED`。

---

## 8. 常用 Skills

| Skill | 何时用 |
|-------|--------|
| `grill-me-codex` | 高风险方案逐问锁定决策树 |
| `create-prd` | 新建/大改 PRD |
| `codex-review` | 已有 PLAN，要对抗评审 |
| `intelligent-selection-pm` | 写选品提案、AI 建议、机会池规则、AT 口径答疑 |
