# 内部数据选品建议引擎 PRD

## 1. 项目背景

商品选品目前主要依赖运营人工判断历史销售报表，缺少一个能把内部商品池的销量、行为、退货、设计属性数据系统化转成可解释选品信号的工具。

内部数据选品建议引擎基于公司已有商品池的销量、用户行为、退货和盘古设计属性数据，识别哪些设计属性已经验证有效、该扩大覆盖，哪些属性高覆盖但表现不佳、存在风险，并把结论产出为结构化的报表与建议清单。

## 2. 项目目标

### 2.1 业务目标

- 把"哪些设计属性卖得好、该扩产、该精简"从人工判断变成结构化、可追溯的信号。
- 用四象限框架交叉"内部适配度"与"覆盖程度"，识别扩展机会与冗余风险。
- 作为下游选品提案的内部验证依据，不替代最终选品决策。

### 2.2 产品目标

新增模块：**内部数据选品建议引擎（Merchandising Intelligence）**，包含以下页面：

- 总览 Dashboard
- 属性钻取
- 空档地图
- 建议清单
- 明细追溯
- 报表中心

## 3. 使用角色


| 角色        | 使用目的                        |
| --------- | --------------------------- |
| 商品运营/选品人员 | 查看哪些设计属性已验证、该扩展或精简，作为选品判断依据 |
| 商品企划      | 查看属性维度趋势和四象限分布，规划下阶段开发方向    |
| 商品运营负责人   | 审阅建议清单和周期报表，决定采纳/拒绝/观察      |
| 数据/开发团队   | 维护属性字典、指标计算逻辑、评分权重、报表生成任务   |
| 管理层       | 通过报表中心快速查看周期核心结论            |




## 4. 分析维度

当前支持七类设计属性：颜色、廓形、后背款式、面料、裙摆/拖尾、领型、袖型；支持三种数据粒度：SKC 粒度、单属性值粒度、属性组合粒度。


| 设计属性                | 现状                           |
| ------------------- | ---------------------------- |
| 颜色 Color            | 已实现                          |
| 廓形 Silhouette       | 已实现                          |
| 后背款式 Back Style     | 已实现                          |
| 面料 Fabric           | 已实现                          |
| 裙摆/拖尾 Hemline/Train | 已实现                          |
| 领型 Neckline         | 已实现                          |
| 袖型 Sleeves          | 已实现                          |
| 是否吊带 Strapless      | 缺失，待补充（与袖型可能存在层级关系，需先确认字段边界） |
| 长度 Length           | 缺失，待补充（命名不统一，可能与裙摆/拖尾语义重叠）   |




## 5. 核心指标与评分逻辑



### 5.1 核心指标

销量、销售额、动销率、加购率、收藏率、退货率、当前覆盖 SKC 数、趋势方向、内部适配度分数（0-100）。

### 5.2 内部适配度打分公式

```text
内部适配度 = 销量表现×30% + 动销率×20% + 加购率×15%
           + 收藏率×10% + 趋势表现×15% + 低退货表现×10%
```

六个子指标先归一化为 0-100 再加权，权重可在设置中调整，总和须为 100%，页面上展示每项的分数贡献。

- **销量表现**：卖了多少，总量指标。
- **动销率**：铺了多少款里真正在动的比例，效率指标。
- **加购率**：加购/浏览，购买意向的领先信号。
- **收藏率**：收藏/浏览，意向强度略弱于加购。
- **趋势表现**：近 N 周环比/同比增速，是否越来越受欢迎。
- **低退货表现**：退货率越低分越高，过滤"销量高但退货率也高"的虚假受欢迎信号。

价格折扣、浏览量、库存深度、评论情感等指标尚未纳入评分公式，后续可视数据情况扩展。

### 5.3 四象限决策框架

横轴：当前覆盖 SKC 数；纵轴：内部适配度分数。默认阈值：适配度 70 分、覆盖 12 个 SKC，可在「空档地图」页手动调整。


| 象限   | 条件       | 含义             | 典型建议动作                |
| ---- | -------- | -------------- | --------------------- |
| 重点机会 | 高分 × 低覆盖 | 已验证但覆盖不足，最值得扩展 | 扩配色/扩尺码/加关联款/加库存深度    |
| 成熟优势 | 高分 × 高覆盖 | 已验证且覆盖充分       | 维持现有规模                |
| 风险冗余 | 低分 × 高覆盖 | 表现不佳但占用较多资源    | 精简 SKC/降低补货优先级/排查退货问题 |
| 持续观察 | 低分 × 低覆盖 | 暂不构成明显机会或风险    | 暂不采取明确动作              |




## 6. 选品建议生成逻辑（六步链路）


| 步骤          | 输入                      | 处理逻辑                                  | 输出            |
| ----------- | ----------------------- | ------------------------------------- | ------------- |
| ① 统一主键关联    | SPU/SKC/SKU 编码 + 盘古设计属性 | 按 external_goods_id + color 关联款式与设计属性 | 属性齐全的 SKC 主数据 |
| ② 指标聚合      | 订单明细、加购/收藏行为、退货原因       | 按单属性值/属性组合两个粒度聚合                      | 属性级指标表        |
| ③ 趋势识别      | 属性级指标的历史序列              | 近 N 周环比/同比，判断上升/稳定/下降及强度              | 趋势方向与强度标记     |
| ④ 内部适配度打分   | 动销、趋势、加购收藏、退货风险四组指标     | 套用 5.2 的加权公式                          | 内部适配度分数       |
| ⑤ 覆盖对比与象限划分 | 适配度分数 + 当前覆盖 SKC 数      | 落入 5.3 的四象限                           | 象限归类          |
| ⑥ 建议生成      | 象限归类 + 具体 SKC 证据        | 按象限规则映射建议类型与动作，拼出可读推理链，禁止无依据编造        | 结构化建议 + 证据链   |


每条建议必须能展开看到证据（趋势数值、评分拆解、关联 SKC、退货明细），保证可解释、可追溯、可人工纠偏，而非端到端黑箱模型。

## 7. 功能需求



### 7.1 总览 Dashboard

核心 KPI 卡片、近 12 周机会/风险趋势、各属性维度表现概览、Top 机会点表格、最新建议列表，可一键进入报表中心生成本周期报表。

### 7.2 属性钻取

按维度查看属性值排行，支持筛选和排序；点击可查看趋势、评分拆解、关联 SKC，并加入建议清单。

### 7.3 空档地图

四象限散点图（横轴覆盖数，纵轴适配度，点大小代表销量），支持按象限筛选，支持调整适配度/覆盖数阈值并实时重新归类。

### 7.4 建议清单

系统生成的结构化建议列表，支持按状态/类型/象限筛选，可标记讨论状态、指定负责人、查看证据链。

### 7.5 明细追溯

按建议下钻查看关联 SKC、销售/行为/退货明细和完整证据链，避免建议成为黑箱。

### 7.6 报表中心

按统计周期生成、查看、下载第 8 节定义的多表报表（XLSX/CSV），展示"本期核心发现"摘要。

## 8. 最终报表产出

本模块的最终产出是一份按统计周期导出的结构化多表报表，通过报表中心查看和生成，共 5 张表：


| 序号  | 表名         | 对应页面      | 定位                    |
| --- | ---------- | --------- | --------------------- |
| ①   | 概览指标摘要表    | 总览        | 一行/期的整体 KPI 快照        |
| ②   | 属性钻取排行表    | 属性钻取      | 核心表，全维度属性值排行          |
| ③   | 象限变动表      | 空档地图（周对周） | 追踪属性值象限迁移，当前未实现，需新增开发 |
| ④   | 选品建议清单表    | 建议清单      | 核心决策表，运营实际要处理的对象      |
| ⑤   | 关联 SKC 明细表 | 明细追溯      | 支撑证据，按需下钻导出           |


五张表通过属性 ID、建议编号、SKC 编码互相关联，可整份导出，也可只导出某个属性或建议关联的子集。

### 8.1 各表详细说明



#### ① 概览指标摘要表

- **粒度**：每个统计周期（周/月）一行，是全模块的"封面摘要"。
- **字段**：统计周期、总销量、总销售额、整体动销率、整体加购率、整体收藏率、整体退货率、当前覆盖 SKC 总数、四象限分布计数（重点机会/成熟优势/风险冗余/持续观察各多少个属性值）、环比变化（较上周期增减）。
- **用途**：管理层/运营负责人不用打开其他表，一行就能看懂"这期整体是变好还是变差"，对应总览 Dashboard 的核心 KPI 卡片。



#### ② 属性钻取排行表

- **粒度**：单属性值 + 属性组合两个粒度，覆盖当前 7 类设计属性（颜色、廓形、后背款式、面料、裙摆/拖尾、领型、袖型）。
- **字段**：属性维度、属性值（或组合）、销量、销售额、动销率、加购率、收藏率、退货率、当前覆盖 SKC 数、趋势方向与强度（近 N 周环比/同比）、内部适配度总分及 6 个子指标贡献度拆解（销量表现/动销率/加购率/收藏率/趋势表现/低退货表现各自得分）、当前所属象限。
- **用途**：本模块的核心底表，其他表（③④⑤）基本都是从这张表派生或关联出来的。支撑属性钻取页面的排行、筛选、排序，运营/企划靠这张表判断"哪个属性值该扩、哪个该精简"。



#### ③ 象限变动表（当前未实现，需新增开发）

- **粒度**：属性值/属性组合 × 周期对比（本周期 vs 上一周期）。
- **字段**：属性值/组合 ID、上一周期象限、本周期象限、迁移方向（如"持续观察→重点机会"）、迁移原因摘要（适配度分数变化量、覆盖 SKC 数变化量）、是否触发关注（迁移到风险冗余或迁移出重点机会时高亮）。
- **用途**：对应空档地图页面的"周对周"追踪能力，回答"谁在变热、谁在变冷"，而不只是看某一周的静态快照。依赖 P1 要落地的周期快照能力（需先把每期的属性钻取排行表结果存档，才能算出周期间差异）。



#### ④ 选品建议清单表

- **粒度**：一条建议 = 一个建议编号，对应一个属性值/组合在某象限下产出的一条动作建议。
- **字段**：建议编号、关联属性维度/属性值、当前象限、建议类型（扩配色/扩尺码/加关联款/加库存深度/维持现有规模/精简 SKC/降低补货优先级/排查退货问题/暂不采取动作）、证据摘要（适配度分数、覆盖 SKC 数、趋势标记）、处理状态（待处理/讨论中/已采纳/已拒绝/观察中）、负责人、状态更新时间。
- **用途**：运营团队实际要处理的工作清单，对应建议清单页面，支持按状态/类型/象限筛选、指派负责人、跟踪处理进度，是从"分析结论"到"人工决策落地"的转换表。



#### ⑤ 关联 SKC 明细表

- **粒度**：一行 = 一个 SKC，按建议编号或属性值展开后的最细粒度证据。
- **字段**：SKC 编码、SPU 编码（`external_goods_id`）、关联的建议编号/属性值、销量、销售额、动销状态、加购数、收藏数、退货数及退货原因（一级/二级）、上架时间、当前库存深度（若数据可获取）。
- **用途**：支撑第 6 节"证据链"要求——每条建议展开后要能看到具体是哪些 SKC 在拉动数据，对应明细追溯页面，按需下钻导出，避免建议清单沦为"黑箱结论"。



## 9. 数据来源



### 9.1商品主数据


| **系统**       | **数据库/表**                             | **可获取字段**                                                                                                                                                                            | **接口**                                                                              | **备注**                                      |
| ------------ | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | ------------------------------------------- |
| **盘古 Pangu** | `pangu`库商品主表                          | SPU编码（款式级）                                                                                                                                                                           | `pangu-api` `AttributeController`等，具体商品主表接口需盘古团队确认                                  | 盘古是设计属性和款式的源头系统                             |
| **ERP**      | `ecshop.ecs_goods`                    | SKC编码（`goods_id`自增主键，对应款式+颜色）、`external_goods_id`（外部货号/SPU）、`uniq_sku`（SKU编码，含颜色/尺码编码段）、`market_price`（吊牌价）、`shop_price`/`shop_price_exchange`（成交价原始/汇率换算）、`cat_id`/`top_cat_id`（品类） | 无独立REST接口，走`azwebapp/Services`层或直接DB查询                                              | 一行=一个SKC(颜色款)                               |
| **ERP**      | `ecshop.ecs_goods_style`              | SKU编码级尺码变体（`style_id`+`goods_id`组合）                                                                                                                                                  | 同上                                                                                  | 一行=一个SKU                                    |
| **ERP**      | `ecshop.skc_on_sale_info`             | 上架时间（`first_on_sale_time`，精确到SKC级）、`on_sale_status`（上架状态）                                                                                                                            | 同上                                                                                  | 专门的SKC级上架管理表                                |
| **ERP**      | `ecshop.ecs_goods`（`is_on_sale`字段）    | 下架状态（0/1，无具体下架时间戳字段）                                                                                                                                                                 | 同上                                                                                  | ⚠️无独立下架时间字段，只有状态位                           |
| **ERP**      | `ecshop.ecs_order_goods_discount_fee` | 折扣类型（`discount_type`）                                                                                                                                                                | 同上                                                                                  | ⚠️这是订单维度属性，非商品主数据固定属性                       |
| **ERP/网站**   | 站点/渠道                                 | US/UK/DE/FR/AU站点字段、销售渠道（正式/样衣/现货）                                                                                                                                                    | 需在`ecs_order_info`（站点/国家字段，如`region`/`country_id`）及`ecs_goods`（`is_sample`等样衣标识）中确认 | 样衣标识散落在多处（如`isPureSample`），建议按具体业务场景二次确认字段名 |




### 9.2 设计属性数据


| **系统**       | **数据库/表或接口**                                   | **可获取字段**                                                                                                                            | **接口**                                          | **备注**                                    |
| ------------ | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------- | ----------------------------------------- |
| **盘古 Pangu** | `pangu`库属性字典表 + `mps.editor_goods_attr`（同步落地表） | 廓形(Silhouette)、后背款式(Back Style)、面料(Fabric)、裙摆/拖尾(Hemline/Train)、是否吊带(Strapless)、长度(Length)、领型(Neckline)、袖型(Sleeves/Straps & Sleeves) | `GET /attributes`（`AttributeController::all()`） | 属性同步机制见`Helper.php`的`SYNC_NEW_ERP_ATTR`常量 |
| **盘古 Pangu** | `Attribute`/`CategoryAttribute`/`languages`关联表 | 各属性的中英文取值枚举 + 脏数据黑名单（`NOT_SYNC_ID`）                                                                                                  | 同上接口                                            | **已清洗的枚举字典源**，建议直接找盘古团队要全量导出              |
|              |                                                |                                                                                                                                      |                                                 |                                           |




### 9.3 销售数据


| **系统**       | **数据库/表**                                                              | **可获取字段**                                                                                                                                                                                                  | **接口**                                                                                                                                   | **备注**                          |
| ------------ | ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| **ERP**      | `ecshop.ecs_order_goods`                                               | 订单编号(`order_id`)、购买数量(`goods_number`)、成交单价(`goods_price`/`shop_price`)、吊牌价(`market_price`)、发货数量(`send_number`)、是否赠品(`is_gift`)、是否实物(`is_real`)、SKU(需JOIN`ecs_goods.uniq_sku`)、款式/尺码(`goods_id`/`style_id`) | 无独立REST接口，DB直查或BI报表                                                                                                                      | 订单-SKU级明细，最细粒度                  |
| **ERP**      | `ecshop.ecs_order_info`                                                | 下单时间(`create_time`)                                                                                                                                                                                        | 同上                                                                                                                                       | 与`ecs_order_goods`按`order_id`关联 |
| **BI az-bi** | `nebulas`库（`app_dresses_ddid_sale_rank`、`app_dresses_color_sale_rank`） | 预聚合的款式/颜色销量排行                                                                                                                                                                                              | `GoodsSkcSkuSalesController::getData()`（支持`start_at/end_at/country/platform/category/data_level`参数，`data_level`可选Category/Goods/SKC/SKU） | 现成的分层销售报表接口，历史可回溯至2017年起        |




### 9.4 用户行为数据


| **数据需求**       | **系统**    | **库/表**                                                   | **备注**                              |
| -------------- | --------- | --------------------------------------------------------- | ----------------------------------- |
| 加购记录（商品+用户+时间） | CMS zzcms | `shopping_cart`表（`CartService::getCartGoodsIdByUserId()`） | 仅当前购物车快照+软删标记，非完整事件流水，历史加购事件需另查变更日志 |
| 收藏/心愿单记录       | CMS zzcms | `show_room_goods_favorites`表                              | 用户级+商品级明细                           |
|                |           |                                                           |                                     |




### 9.5 退货/退款数据


| **数据需求**        | **系统**       | **库/表**                                                                               | **备注**               |
| --------------- | ------------ | ------------------------------------------------------------------------------------- | -------------------- |
| 退货数量/退货率（消费者口径） | ERP azwebapp | `Services/Refund/`相关表（`return_apply`、`ecs_order_goods`关联）                             |                      |
| 退货/退款原因（一级+二级）  | ERP azwebapp | `RomeoApi/lib_refundV2.php`中`REFUND_REASON_CATEGORY_AZ`等常量 + 对应`romeo.refund_reason`表 | 具体二级分类字段需读取该常量完整定义确认 |
| 退货关联SKU/SKC     | ERP          | `ecs_order_goods.uniq_sku`（通过退款单关联订单商品行）                                              | 可精确到SKU              |
| 退货发生时间          | ERP          | 退款单据的创建/审批时间字段（在`return_apply`或`RefundBlockRuleService`关联表中）                          | 需二次确认具体时间字段名         |




### 9.6 库存数据


| **数据需求**            | **系统**       | **库/表**                                                             | **备注**                                                                                                 |
| ------------------- | ------------ | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 库存深度                | ERP azwebapp | `Services/Inventory/`相关表（如`ecs_goods`关联的库存数量字段，或`stock_order_info`） | 需二次确认具体字段名（如`quantity_on_hand`类字段），当前搜索中`quality_management`路径下曾出现`quantity_on_hand_di`（Inventory相关方法） |
| 周转天数                | ERP          | 报表类计算指标，可能在`ad_supply_chain`系列报表中现成计算                               | 未直接定位到独立字段，大概率是报表层计算值而非商品主数据固定字段，需二次确认                                                                 |
| 库存状态（在售/低库存/缺货/已下架） | ERP          | 结合`ecs_goods.is_on_sale`（在售/下架）+ 库存数量字段联合判断                         | 系统内可能没有独立"低库存/缺货"状态字段，是业务层按库存数阈值实时计算得出的派生状态，不是存储字段                                                     |




## 10. MVP 范围



### P0（已完成）

### **[http://10.180.248.185:5173/traceability（是一个小demo）](http://10.180.248.185:5173/traceability)**

- 五个业务页面完整交互、四象限归类逻辑与评分拆解，当前数据源为模拟数据。



### P1

- 接入真实内部数据源（见第 9 节数据来源）。
- 后端化评分计算与建议生成，替换前端模拟逻辑。
- 落地周期快照能力，支撑报表中心的象限变动表（表③）。
- 上线报表中心页面，支持查看、下载 XLSX/CSV。



### P2

- 开放数据接口，供选品提案生成层等下游系统消费。
- 支持与其他数据源共享统一属性字典，便于跨系统信号整合。
- 报表中心新增 AI 叙述性摘要生成能力。

