# 上新表 / 下架表逻辑说明

> 适用范围：BD / Atelier 各站点爬取完成后的 Excel 分表（`{站点}_原始排序表` / `{站点}_上新表` / `{站点}_下架表`）。  
> 核心实现：`common/baseline_manager.py`、`common/report_builder.py`、`common/report_history.py`。

---

## 1. 核心结论

**「上周 vs 本周」不是按日历周做 SQL / 日期区间对比**，而是：

```text
本轮全量爬取结果  vs  上一轮持久化基线（JSON）
```

业务上通常每周一跑一轮（见 `scripts/crontab.example`），因此口头称为「周对周」；算法本身只认 **上一轮 / 本轮**。

| 口头说法 | 实际含义 |
|---------|---------|
| 本周 | 当前这一轮爬取结果 |
| 上周 | 上一轮写入的基线快照 |
| 数据周次 | 爬取日的 ISO 周标签 `YYYY-Www`（仅作行级标注） |

`common/report_history.py` 明确约定：

> 不再按周/小时判断是否更新；只要运行一次，就和上一轮基线做对比。

---

## 2. 对比单位（SKC Key）

生命周期判断的唯一键：

```text
normalize(product_key) ::: normalize(color_name)
```

- 空白归一、大小写 `casefold`（如 `Dusty Blue` / `dusty blue` 视为同一颜色）
- 基线按 **颜色级 SKC** 维护状态：`Active` / `Delisted`
- 展示字段不受 key 标准化影响，仅生命周期判断使用标准化 key

基线文件位置（不入 git）：

```text
baselines/bd/*_baseline.json
baselines/atelier/*_baseline.json
```

单条记录大致结构：

```json
{
  "first_seen": "YYYY-MM-DD",
  "last_seen": "YYYY-MM-DD",
  "status": "Active | Delisted",
  "delisted_at": "YYYY-MM-DD | \"\"",
  "metadata": { "current_rank": 12, "new_type": "...", "...": "..." }
}
```

---

## 3. 爬取完成后的处理流程

```text
全量抓取本站 Active SKC
        │
        ▼
加载 BaselineManager（JSON）
        │
        ├── 初始化阶段？
        │     · 是 → check_and_update 后强制 is_new_color =「基线写入」
        │     · 否 → 正常返回「是 / 否 / 老款补货」
        │
        ▼
收集本轮 active_keys
        │
        ▼
mark_missing_as_delisted（缺失的 Active → Delisted）
        │
        ▼
原子写入 save_baseline()
        │
        ▼
build_report_sheets → Excel 三表
        │
        ├── {站点}_原始排序表
        ├── {站点}_上新表
        └── {站点}_下架表
        │
        ▼
（可选）GSheet 同步 / 周报 AI / 清理旧 Excel（只保留最新一轮）
```

代表调用链（各站点 `main_*.py` 模式一致）：

1. `is_first_site_crawl` / `resolve_diff_initialization_phase` → 是否初始化
2. 抓取商品列表
3. 逐 SKC：`apply_ranking_context` → `check_and_update`
4. `mark_and_build_delisted_records(..., min_active_count=...)`
5. `save_baseline()`
6. `build_report_sheets(...)` → `DataExporter.export_multiple_sheets`
7. `cleanup_previous_site_reports`（同站点只留最新 Excel）

---

## 4. 上新表逻辑

### 4.1 入表条件

`is_new_color` 属于：

```text
{"是", "老款补货"}
```

（定义见 `report_builder.NEW_STATUS_VALUES`）

初始化阶段即使跑了 `check_and_update`，也会把标记改成「基线写入」，**上新表行数为空**，避免首次全量误判为上新。

### 4.2 分类口径（`BaselineManager.check_and_update`）

| 返回 `is_new_color` | 上新类型 `new_type` | 判定条件 |
|--------------------|---------------------|---------|
| `是` | `新 SKC` | key 从未出现，且同款 `product_key` 也不在基线 |
| `是` | `新颜色` | key 从未出现，但同款已有其他颜色 |
| `老款补货` | `下架后又上新` | key 曾为 `Delisted`，本轮再次抓到 |
| `否` | （空，不入上新表） | key 上一轮已是 `Active` |

### 4.3 上新表主要字段含义

| 字段 | 含义 |
|------|------|
| 排序 | 本周官网列表展示序 |
| 上新类型 | 新 SKC / 新颜色 / 下架后又上新 |
| 上新时间 | `first_seen` |
| 最近下架时间 | 补货场景才有（上次 `Delisted` 时间） |
| 数据周次 | 爬取日 ISO 周 `YYYY-Www` |

---

## 5. 下架表逻辑

### 5.1 判定公式

本轮完整抓取后：

```text
newly_delisted =
  基线中 status == Active
  AND key ∉ 本轮 active_keys
  AND（若提供 eligible_keys，则 key 必须在其中）
```

随后：

- `status` → `Delisted`
- `delisted_at` = 本轮爬取日

**下架表只输出「本次新下架」**，不是历史全部 `Delisted` 集合。

### 5.2 下架表主要字段含义

| 字段 | 含义 |
|------|------|
| 下架前排序 | 基线里上次保存的 `metadata.current_rank` |
| 最近一次出现时间 | `last_seen` |
| 下架时间 | 本轮爬取日 |
| 下架前标价/售价/折扣 | 来自基线 metadata 快照 |
| 数据周次 | 爬取日 ISO 周 |

### 5.3 防误下架安全阀

| 规则 | 作用 |
|------|------|
| `min_active_count` | 本轮 Active 数低于阈值则**整站跳过**下架标记 |
| 空爬 / 抓取失败 | 不写基线、不做下架，避免半成品覆盖 |
| `eligible_keys`（House of CB） | 仅对窗口内 key 做下架，避免列表缩短误杀 |
| Club L soft fail | 详情页 404/503 等软失败，不单独作为下架依据 |

默认阈值（`common/config.py`，可用环境变量覆盖）：

| 站点 | 默认 `min_expected_active` |
|------|---------------------------|
| Birdy Grey / Six Stories / Club L | 200 |
| Babyboo / Hello Molly / Azazie | 100 |

历史 `Delisted` 可通过 `purge_old_delisted(keep_days=...)` 清理，默认保留天数由 `DELISTED_KEEP_DAYS`（默认 180）控制。

---

## 6. 原始排序表中的「上周 / 本周」排名

在 `apply_ranking_context` 中：

| 字段 | 取值 |
|------|------|
| 本周排序 | 本轮列表序 `current_rank` |
| 上周排序 | 上一轮基线 `metadata.current_rank`（及兼容别名） |
| 排名涨跌 | `上周排序 - 本周排序` → `+N` / `-N` / `+0`；新品或缺一侧时留空 |

---

## 7. 初始化与特殊规则

### 7.1 首次爬取 / 空基线

- 只写入基线，建立生命周期起点
- 上新表、下架表 **sheet 存在但为空**
- 避免「第一次跑就把全站当成上新」

### 7.2 Azazie 基线未成熟

`resolve_diff_initialization_phase`：若 Azazie SPU 数 `< AZ_MIN_STYLES_FOR_DIFF`（默认 **400**），仍按初始化处理，防止冒烟/小样本后出现海量假上新。  
修复工具：`scripts/azazie_repair_diff_state.py`。

### 7.3 House of CB 窗口 diff

- `diff_window = min(本轮列表长度, 上轮列表长度)`
- 下架仅对上一轮排名 ≤ 窗口的 key 生效（`eligible_keys`）
- 上新/下架表可用 `diff_rank_limit` 再按排名过滤

### 7.4 Style-grouped 导出

- 基线仍按 **颜色级**
- 原始排序表可做款式合并，并展示「本周新增颜色」
- **上新表 / 下架表仍是颜色级行**

### 7.5 老款补货

曾下架的 SKC 再次出现 → 进入上新表，类型为「下架后又上新」，并带「最近下架时间」。

### 7.6 PDP 颜色回填

上一轮列表缺色、下一轮 PDP 补全颜色时，可能集中出现一批「新颜色」上新（属数据补全，不是官网真上新）。相关说明见 Azazie 修复脚本注释。

---

## 8. 爬取完成后的数据去向

| 产物 | 位置 / 用途 |
|------|------------|
| 基线 JSON | `baselines/{bd\|atelier}/*_baseline.json`，供下一轮对比 |
| Excel 三表 | `output/{bd\|atelier}/*_report_*.xlsx` |
| Google Sheets | 站点全量 + `{前缀}_本次上新` / `_本次下架`，并追加「竞品变动历史」 |
| 周报 | `scripts/weekly_send_job.py` 读最新 Excel → AI 汇总上新/下架 |
| 只保留最新 Excel | 同站点导出成功后删除旧 `*_report_*.xlsx` |

周报发送会校验 Excel 新鲜度（`WEEKLY_MAX_EXCEL_AGE_HOURS`，默认 12h），再调用 `summarize_new_listings` / `summarize_delistings`。

---

## 9. 一句话心智模型

```text
上新 = 本轮出现，但上一轮基线中不是 Active
       （或曾 Delisted 又回来 → 老款补货）

下架 = 上一轮 Active，但本轮全量抓取中消失
       （受 min_active_count / eligible_keys / 空爬保护约束）

「周」= 调度节奏（周一任务）+ 行上 ISO 周标签
       ≠ 按自然周日期范围做 join
```

---

## 10. 相关文件索引

| 路径 | 职责 |
|------|------|
| `common/baseline_manager.py` | SKC 生命周期：上新判定、下架标记、基线读写 |
| `common/report_builder.py` | 三表组装、排名上下文、ISO 周次 |
| `common/report_history.py` | 初始化判断、旧报表清理、运行时间解析 |
| `common/data_exporter.py` | 列名别名与 Excel 导出 |
| `common/gsheet_sync.py` | 同步本次上新/下架到 Google Sheets |
| `common/config.py` | 各站 `min_expected_active`、Azazie diff 门槛等 |
| `bd/sites/main_*.py` / `atelier/sites/main_*.py` | 各站爬完后的统一 diff 调用 |
| `scripts/weekly_crawl_job.py` | 周一爬取调度 |
| `scripts/weekly_send_job.py` | 读 Excel 发周报 |
| `scripts/azazie_repair_diff_state.py` | Azazie 误上新状态修复 |
| `bd/ai_assistant/weekly_report_generator.py` | BD 周报上新/下架摘要 |
| `atelier/ai_assistant/weekly_report_generator.py` | AT 周报上新/下架摘要 |
