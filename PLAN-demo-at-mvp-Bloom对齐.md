# Plan: AT Demo（妈妈版）对齐 Bloom · 增删锁定
_Locked via grill — by Claude + 用户_  
_日期：2026-07-29_  
_权威：本计划约束 **选品工作台/demo-at-mvp** 改版；产品合同需同轮升版 PRD（见下）。不覆盖根目录 [PLAN.md](./PLAN.md)（AT MVP 工程计划）。_

## Goal

以 **Owner 对内验收** 为用途，在妈妈版 `demo-at-mvp` 上吸收 Bloom 的合同面（五信号、总览 KR、映射、回执），保留妈妈版运营手感（六频道监控、三场景选品管理、方案右栏 AI 全文），并与 PRD **同轮升版**，避免「demo 有、合同无」。

## Approach

1. **基座**：继续改 `选品工作台/demo-at-mvp.html`（+ css），不以 Bloom 整页替换。  
2. **PRD 同改**：三信号 → 五信号；废除 `recency_proxy` 分池叙事，改为「热卖榜主爬 + 上新榜轻量 WoW」。  
3. **IA**：总览 → 监控(6) → 选品管理(3) → 方案(左表单+右 AI) → 审批与选款(含历史回执) → 属性映射。  
4. **删**：独立「AI选款分析」路由；监控 BD 站分组。  
5. **不加**：Bloom `flow.html`；独立「写入选款回执」菜单；方案内第二套 Gates UI。

## Key decisions & tradeoffs

| # | 决策 | 选择 | 备注 |
|---|------|------|------|
| 1 | Demo 用途 | Owner 对内验收 | 骨架跟合同，手感借妈妈版 |
| 2 | 信号范围 | **五信号正式** | 雷达 · 广告 · 邮件 · 社媒 · AZ 相似；流行趋势为第 6 监控频道，**不进**五源健康度分母 |
| 3 | 与 PRD | **Demo + PRD 同轮升版** | 需 Owner 审阅位 |
| 4 | 机会池 | = 妈妈版「选品管理」三场景 | 纯新款 / 负向改款 / 正向改款；**不**采用 Bloom 四 Tab |
| 5 | 爬取口径 | 热卖榜主爬 + 上新轻量 WoW | **不再**按站点 `recency_proxy` 隔离；PRD 旧表述删除/改写 |
| 6 | 纯新款控件 | 三排序互斥 + 一筛选 | 热销榜 rank · 上新检出时间 · 广告力度(播放×天数) · **邮件主推=勾选筛选**；默认热销榜 |
| 7 | 正/负向控件 | 不用纯新款那套 | 用场景自身排序（VOC/退货/站内销量等） |
| 8 | 合同页吸收 | 总览 KR/漏斗 · 属性映射 · 回执能力 ·（概念）Gates | 回执嵌在审批页下方，不单开菜单 |
| 9 | AI 信息架构 | **取消独立 AI 页** | 生成/打开方案 → 右侧栏 = **妈妈版 AI 选款分析全部内容** |
| 10 | Gates | **不单独做 Gates 块** | 妈妈版多维表即 Gates 数据源；文案可标「事实/门禁」 |
| 11 | 监控频道 | **六个都要** | 流行趋势 · 竞品网站 · 广告 · **邮件(新建)** · **社媒(独立)** · 站内爆款 |
| 12 | BD | **删除** | 只留 AT 站 |
| 13 | 审批与回执 | 单页「审批与选款」 | 上：审方案；下：历史方案/款 + 流转状态（Bloom 回执） |
| 14 | 审批权限 | **仅负责人** | 通过 / 驳回 / 备注；运营只读该页操作区 |
| 15 | 属性映射 | 侧栏独立 | **全员可维护**（不设写权限门禁） |
| 16 | 账号 / 上下文 | 左下角账号模块点开 | 切 **身份 + 品类 + 市场**；删除原「品类运营\|负责人」小双钮 |
| 17 | 品类谁能切 | **仅管理员** | 运营/负责人锁 AT；管理员可选品类（BD 等可「接入中」） |
| 18 | 市场谁能切 | 全员（看数口径） | 对齐 Bloom 顶栏市场；机会/排名按市场过滤，跨市场不混排 |
| 19 | 站点归属维护 | **侧栏独立「站点与市场配置」** | 表：站点 · 品类线 · 主营市场(可多选) · 启用；**仅管理员可写**，他人只读或藏入口 |
| 20 | 演示身份 | 运营 / 负责人 / **管理员** | 管理员 = 最高权限演示位（品类切换 + 站点配置写） |
| 21 | 正向改款发现层 | **竞品热卖属性榜 → 机会列表** | 正向 Tab **上榜下列**；集中度（热销占比 vs 全站）≥阈值才上榜；点属性可筛机会；方案带出叠加属性。站内「喜欢点」不作为本 Tab 主路径（可仍在监控·站内爆款） |
| 22 | 负向改款卡 | **对齐 Bloom 全套 + 池顶警示** | 卡面：负反馈证据（退货率 vs AT P75 · VOC 次数 · 翻车）+ 销量分布(US/UK/AU，跟市场切换) + 改款方向 + 信号条(AD/EDM/翻车/内部) + 生成方案；池顶 notice 定义负向口径 |
| 23 | 监控筛选栏 | **AT 精简知衣版** | 站点 chips（AT 白名单）· 设计细节(面料/廓形/领型/袖长/裙长/颜色/图案，对齐映射) · 上架/检出时间快捷档 · 状态/有广告 · **已选筛选条**；**不做**竞品库存变化、智能合并/跨区合并；复色/大码/价格变化本期不做（要则另开） |

### 导航目标态

```text
总览摘要（KR / 漏斗 / 五源健康度）
监控报告
  ├ 筛选栏（知衣精简：站点 · 设计细节 · 上架时间 · 状态/广告 · 已选条）
  ├ 流行趋势
  ├ 竞品网站（仅当前品类线白名单站）
  ├ 广告投流
  ├ 竞品邮件主推      ← 新建
  ├ 社媒舆情          ← 独立频道
  └ 站内爆款
选品管理
  ├ 纯新款（三排序 + 邮件勾选）
  ├ 负向改款（池顶警示 + Bloom 式证据/分布/改款方向/信号条卡片）
  └ 正向改款：上「竞品热卖属性榜」→ 下「底座×叠加」机会列表
方案编辑（左：方案字段；右：妈妈版 AI 全文）  ← 原 AI 路由并入
审批与选款（负责人可审；下方历史流转/回执）
属性映射表
站点与市场配置          ← 新建（主数据；管理员可写）

左下角账号（点开）
  ├ 身份：运营 / 负责人 / 管理员
  ├ 品类：仅管理员可改（默认 AT）
  └ 市场：全员可切（全部 / US / UK / AU …）
```

### 增删一览

| 动作 | 项 |
|------|-----|
| **增** | 总览；邮件频道；社媒独立频道；纯新款三排序+邮件勾选；**正向改款内竞品热卖属性榜**；**负向改款 Bloom 式卡片+池顶警示**；**监控知衣精简筛选栏**；属性映射；审批页下方历史回执；账号菜单（身份/品类/市场）；站点与市场配置；管理员身份；PRD 五信号与爬取口径 |
| **并** | AI选款分析 → 方案右栏；原角色小双钮 → 账号菜单 |
| **删** | AI 独立导航；BD 进池/洗线（非当前品类数据）；Bloom 四池；独立 recency 池；方案内第二套 Gates UI；flow 页（本期）；侧栏底部角色双钮 |
| **留** | 六频道中其余妈妈版深度；三场景；妈妈版 AI 打分/权重/结论全文 |

## Risks / open questions

| 风险 | 说明 | Owner |
|------|------|-------|
| PRD 升版未审就改 demo | 验收仍可能被打回「超合同」 | 产品 Owner：先出 PRD diff 再或并行改 demo |
| 五维加权 vs §0.A Memo | 右栏保留妈妈版算分器，与 PRD「结论骨架」并存 | 升版 PRD 时写明：Memo 骨架为建议输出形态，打分 UI 为辅助；或接受双轨 |
| 广告力度公式未写死 | 「播放×投放天」需一版可解释公式 | 实现前定权重/归一 |
| 映射全员可写 | Demo 可接受；真系统需审计 | 工程 Later |
| 旧 PLAN/PRD 仍写三信号、recency | 文档漂移 | 同轮改 PRD；根 `PLAN.md` 择要回写 |
| 品类切换 vs「本期只验 AT」 | 管理员可切 BD 空态/接入中，避免误当成 BD 已上线 | Demo 文案必须「接入中」 |
| 站点多市场 | 一站多市场时，当前市场筛选如何命中 | 配置允许多选；看数按「站点服务该市场」过滤 |

## Out of scope

- 用 Bloom `index.html` / `flow.html` 替换妈妈版  
- 独立「写入选款回执」菜单、独立 Gates 页  
- BD/MOB 演示、日频爬取、交期大盘、Ins 自动爬  
- 真后端 / 真爬虫接入（仍 Mock）  
- 本计划签核前的代码改动  

## 签核

- [x] 用户确认本 PLAN 可执行（2026-07-29「签核，可以改 demo」）
- [ ] Owner 确认 PRD 五信号 + 爬取口径升版  
- [x] 开始改 `demo-at-mvp`（PRD 正文可并行后补）  
